apiVersion: v1
kind: Service
metadata:
  name: safek-frontend-service
  namespace: default
spec:
  type: NodePort
  selector:
    app: safek-frontend
  ports:
    - name: http
      port: 80
      targetPort: 80
      nodePort: 30080
    - name: metrics
      port: 9113
      targetPort: 9113
